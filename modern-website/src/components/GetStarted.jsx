import React from 'react';
import { arrowUp } from '../assets';

const GetStarted = () => (
  <div 
   //here i left ---->> className={`${styles.flexCenter} w-[140px] h-[140px] rounded-full bg-blue-gradient p-[2px] cursor-pointer`}
  >

  </div>
);

export default GetStarted;
